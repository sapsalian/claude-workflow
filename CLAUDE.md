# Claude 전역 규칙

## 스킬 워크플로우 공통 규칙

`/dev-full`, `/dev-design`, `/dev-impl` 스킬에 공통 적용되는 규칙.

### Q&A 라운드 가이드라인

각 계획 단계(전체 설계, 각 Phase 세부 설계) 전 Q&A 진행. **전체 설계와 세부 설계 모두 3라운드 필수**, 이후에도 파생 고려사항이 있으면 계속 진행.

**Q&A 라운드 시작 전 아래 체크리스트 전체를 검토한 뒤 고려사항 요약 출력:**

#### 전체 설계 시 검토 체크리스트
*아키텍처/정책 수준 — 무엇을 만들 것인가*

- **요구사항/범위**: 구현해야 할 것과 하지 않아도 될 것의 경계, 모호한 표현 명확화
- **사용 시나리오**: 누가, 어떤 상황에서, 어떤 목적으로 사용하는지 구체적 시나리오
- **성공 기준**: 이 기능이 "완료됐다"고 볼 수 있는 조건
- **우선순위**: 반드시 있어야 하는 것 vs 있으면 좋은 것 구분
- **기존 동작 영향**: 변경으로 인해 영향받는 기존 기능
- **아키텍처/기술**: 기술 스택 선택, 패키지 추가 여부, 기존 패턴과 일관성
- **UI/UX**: 레이아웃 구조, 네비게이션 흐름, 반응형 여부, 빈 상태/로딩/에러 상태 처리 방식
- **API/인터페이스**: 엔드포인트 설계, 요청/응답 형식, 버전 관리
- **데이터 모델**: 필드 구조, 타입, 관계, 영속화 방식
- **에러 처리**: 사용자에게 노출할 에러 메시지, 복구 흐름
- **성능/제약**: 페이지네이션, 캐싱, 동시성 처리 필요 여부
- **테스트 전략**: 단위/통합/e2e 범위, 모킹 대상

#### Phase 세부 설계 시 검토 체크리스트
*구현 수준 — Phase N의 각 인터랙션을 어떻게 구현할 것인가*

- **세부 동작 명세**: 각 인터랙션의 정확한 동작 (예: 저장 후 이동? 즉시 반영?), 모호한 요구사항 구체화
- **예외 시나리오**: "만약 X라면?" UX — 빈 값, 중복, 권한 없음 등 경계 상황의 동작
- **UI 컴포넌트**: 컴포넌트 분해 방식, props 인터페이스, 상태 관리 위치
- **UI 상태**: 로딩/에러/빈 상태/성공 상태 각각의 표현 방식
- **사용자 인터랙션**: 클릭/입력/드래그 등 인터랙션 흐름, 피드백 방식 (토스트/인라인/모달)
- **데이터 흐름**: 상위→하위 props 전달 vs 전역 상태 vs 로컬 상태
- **구현 순서**: 의존성 고려한 sub-step 순서, 병렬 가능 여부
- **엣지 케이스**: 경계값, 권한, 중복 요청 방지 등

**고려사항 요약 형식 (라운드마다 필수 출력):**
```
## 고려 사항 정리 (라운드 N)

### 검토한 사항
- [사항]: [판단]

### 자동 결정한 사항 (명백한 기술적 사실만)
- [사항]: [결정] — 이유: [근거]

### 사용자에게 질문할 사항
- [질문들]
```
- **전체 설계·세부 설계 모두 3라운드 필수**: 질문할 사항이 없더라도 3라운드까지 반드시 진행. 이전 라운드 결정이 새로운 고려사항을 파생시킬 수 있음
- **3라운드 이후 연장**: 파생 고려사항이 남아 있으면 4라운드, 5라운드도 계속 진행
- **종료 조건**: 3라운드 완료 후, 요구사항이 구체화되고 파생 고려사항이 없고 모든 기술적 사항이 확정된 경우에만 종료
- **적극적으로 질문**: 자동 결정은 명백한 기술적 사실(언어 선택, 프레임워크 제약 등)에만 한정. 트레이드오프가 있는 모든 결정(UI 방식, 데이터 구조, 동작 방식 등)은 사용자에게 질문할 것

### Phase 구조 원칙

- 각 Phase는 **독립적으로 동작하는 앱/시스템 상태**를 목표로 함
- Phase 완료 조건: 테스트 통과 + 빌드 성공
- **테스트 우선**: 기능 구현 전 테스트를 먼저 작성 (TDD)
- 테스트 인프라가 없으면 Phase 시작 전 구축
- 복잡한 Phase는 sub-step으로 분해 가능 (각 sub-step = 독립 테스트/커밋 단위)
  - plan 파일 추적: `[🔄 진행 중] (완료된 step/전체 step)` 형식

### 커밋 규칙

- **Phase 완료 시 반드시 커밋** (완료 알림 후 즉시 커밋)
- Phase 내 각 기능/수정 단위마다 중간 커밋 진행
- 커밋 메시지: 프로젝트 기존 컨벤션 따름 (conventional commits)
- 커밋 전 프로젝트 CLAUDE.md 업데이트 확인 — 새 파일/패키지 추가, API 변경, 구조 변경 시 반영

### Plan 파일 관리

- 경로: `{프로젝트 루트}/.claude/plans/YYYY-MM-DD-<task-slug>.md`
- Phase 상태 마커: `[✅ 완료]` `[🔄 진행 중]` `[⏳ 대기]`
- `~/.claude/plans/`: plan mode 세션 중 생성. ExitPlanMode 승인 UI에 내용이 표시됨
- 프로젝트 plan 파일: 영구 기록 + Phase 상태 추적 (Claude ↔ Codex 공유)

### Phase 전환 시 강제 확인

ExitPlanMode 직후 또는 Phase 완료 후 자동으로 다음 단계를 진행하지 말 것:
- **반드시 AskUserQuestion 호출** 후 사용자 승인을 받아야 다음 단계 진행
- "yes"를 받아야만 다음 Phase 세부 설계 또는 구현을 시작할 수 있음

## Codex CLI 협업 규칙

Claude Code와 Codex는 **피어 관계**. 양쪽 모두 동일한 4종 스킬을 보유.

| 스킬 | 용도 |
|---|---|
| `dev-full` | Q&A + 설계 + 구현 전 과정 (독립 실행) |
| `dev-design` | Q&A + plan 생성 전담. 구현은 dev-impl에 핸드오프 |
| `dev-impl` | plan 픽업 + 설계 검토 + 구현 |
| `cothink` | plan 모드 없이 Q&A 반복 후 요청 수행 |

### 핸드오프 패턴

**설계와 구현을 분리할 때:**
- Claude Code `/dev-design` → plan 파일의 `세부 설계` 섹션 채우기
- Codex `/dev-impl` → plan 픽업 → 설계 검토 → 구현

**핸드오프 신호**: plan 파일 각 Phase의 `#### 세부 설계` 섹션이 채워진 상태

**Codex → Claude Code 복귀 조건**:
- 복잡한 디버깅 (구조 파악 필요)
- 구조 변경 또는 설계 재검토 필요
- 새로운 Phase 세부 설계 Q&A 필요
